{
  "name": "liquidturn",
  "version": "1.0.0",
  "description": "Sistema de turnos para liquidación de gastos de vendedores Kazen",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  }
}
